# OS / editor cruft
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Local server / dependency artifacts
node_modules/
# jsdom is only needed to run tests/check-pages.js. Installed ad-hoc
# (see README) so the site itself stays dependency-free.
package.json
package-lock.json
